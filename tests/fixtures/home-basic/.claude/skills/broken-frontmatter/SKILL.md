---
name: broken-frontmatter
description: [this flow sequence is never closed
tags: {also: broken
---

# Broken Frontmatter

This sample exists to verify that scan records a parse error instead of crashing.
The YAML above is intentionally invalid (unclosed flow sequence and mapping).
