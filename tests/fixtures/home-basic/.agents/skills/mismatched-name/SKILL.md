---
name: release-runbook
description: Guide the release process from tagging to changelog to announcement. Use when cutting a new release.
---

# Release Runbook

The frontmatter name (release-runbook) intentionally differs from the directory
name (mismatched-name). Per the agentskills spec, name must match the directory;
scan should surface this record and S5 lint should flag it as an error.
